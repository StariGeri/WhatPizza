/**
 * This MW edits an existing pizzas data
 * @param {*} dataRepository
 * @returns
 */
module.exports = function (dataRepository) {
    return function (req, res, next) {
      //TODO: implement this MW
      // get the pizzas data from the database
      // display the pizzas data in the form
      // send the edited data to the database
    };
  };