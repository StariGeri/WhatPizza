/**
 * This MW fetches every pizza by the restaurant
 * @param {*} dataRepository
 * @returns
 */
module.exports = function (dataRepository) {
    return function (req, res, next) {
      //TODO: implement this MW
      // send request to the database to fetch every pizza by the restaurant, using the restaurants id
    };
  };